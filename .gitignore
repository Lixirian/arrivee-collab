# Artefacts de build NON distribués par GitHub (le zip versionné courant et
# latest.json, eux, SONT suivis : ils forment le canal de distribution raw).
Archives/
dist-ready/
Arrivee-Collab-dist-ready.zip
Arrivee-Collab-maj-*.zip

# Restes de tests locaux : SENSIBLE (mots de passe, .msg) — jamais dans le dépôt.
Mot de passe/
Archive message/
*.msg

